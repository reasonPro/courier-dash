# CourierDash — план розвитку та технічний журнал

## 1. Призначення документа

Цей документ є єдиним актуальним джерелом інформації про:

- поточний стан проєкту;
- завершені роботи;
- активну задачу;
- заплановані етапи;
- прийняті технічні рішення;
- відомі проблеми;
- результати перевірок;
- питання, які очікують рішення.

Одночасно активною може бути лише одна технічна підзадача. Після завершення кожного етапу потрібно оновити його статус, результати перевірок і журнал виконання.

## 2. Головні правила безпеки

- Не ламати функціонал, який уже працює.
- Перед кожним завданням фіксувати початковий стан.
- Одна задача — одна окрема feature або fix гілка.
- Не змішувати в одному завданні рефакторинг, виправлення помилок, переклади, зміни бази даних і новий функціонал.
- Якщо є неоднозначність, яка впливає на поведінку, зупинитися та поставити запитання до реалізації.
- Не приймати самостійних рішень щодо фінансових формул, податків, структури бази, даних користувачів, сумісності localStorage або поведінки UI.
- Кожну зміну структури Supabase виконувати через окрему міграцію.
- За можливості зберігати зворотну сумісність зі старими записами.
- Ніколи не використовувати Supabase `service_role` у клієнтському коді.
- Не виконувати commit, merge, push або deployment до ручної перевірки власником проєкту.
- Віддавати перевагу малим послідовним змінам, а не великому переписуванню.
- Не видаляти старий робочий код, доки його заміну не перевірено.
- Не змінювати існуючі localStorage-ключі без окремого рішення та плану сумісності.
- Не змінювати фінансові формули в межах технічного очищення або типізації.
- Перед змінами бази визначати вплив на старі записи, місячний звіт, річний звіт і майбутній мобільний застосунок.

## 3. Ролі

### Власник проєкту

- визначає потрібний функціонал;
- приймає продуктові рішення;
- затверджує важливі технічні рішення;
- вручну перевіряє поведінку сайту;
- дає дозвіл на commit, merge і push.

### ChatGPT

- планує етапи;
- аналізує архітектуру;
- готує промпти для Codex;
- аналізує відповіді Codex;
- допомагає оцінювати ризики;
- координує оновлення roadmap.

### Codex

- аналізує репозиторій;
- виконує тільки затверджене завдання;
- запускає автоматичні перевірки;
- надає структурований звіт;
- не приймає неузгоджених продуктових або архітектурних рішень;
- не виконує commit, merge, push або deployment без окремої команди.

## 4. Завершений функціонал

- Підтримка Stuart.
- Підтримка платформи Other.
- Гнучкий вибір активних платформ.
- Вибрані платформи зберігаються в localStorage.
- Сторінку Work розділено на компоненти.
- Онлайн-чайові та готівкові чайові враховуються окремо.
- Мобільний заголовок чайових налаштовано для української, англійської, польської та російської мов.
- Використовується Git-процес: feature або fix гілка → перевірка → commit → merge у main → push → автоматичний deployment у Vercel.
- Вибрана мова застосунку зараз зберігається між відвідуваннями.
- Завершено контрольовану стабілізацію ESLint малими підзадачами A2.1–A2.11.
- Усунено всі актуальні ESLint errors і warnings; фінальний baseline: 0 errors, 0 warnings.
- Типізовано користувача Supabase через офіційний тип `User`.
- Усунено `explicit any` у погоджених структурах Chart.js за допомогою офіційних типів бібліотеки.
- Виправлено порядок оголошення loader та auth helper функцій без зміни їхньої логіки.
- Проаналізовано browser-only hydration налаштувань полів і вибраної мови; поточну SSR-безпечну послідовність збережено.
- Два свідомі browser-only effects документовано вузькими локальними ESLint-виключеннями без зміни runtime-поведінки.

Garage ще не локалізований повністю. Annual Report ще не локалізований повністю.

## 5. Відомі проблеми

- Garage перекладено не повністю.
- Annual Report перекладено не повністю.
- Annual Report ще не враховує всі поля та значення, які доступні в місячному звіті.
- Повний модуль витрат ще не реалізований.
- Податки та розрахунок Netto ще не завершені.
- Кореневий атрибут `<html lang="pl">` зараз не оновлюється при зміні мови. Це окрема майбутня accessibility/SSR-localization задача, яка не блокує поточне перемикання перекладів у клієнтському UI.
- `courier_field_settings` безпечно обробляє синтаксично пошкоджений JSON, але валідний JSON із неправильною або частковою структурою ще не має окремої schema validation. Це відкладена, не активна задача.
- У майбутньому можуть додаватися нові:
  - розрахунки;
  - фінансові показники;
  - колонки бази даних;
  - платформи;
  - витрати;
  - податкові правила;
  - звіти;
  - функції Garage;
  - мобільний застосунок.

## 6. План робіт

### Фаза A — Стабілізація

- [COMPLETED] A1. Roadmap і повторний базовий read-only аудит
- [COMPLETED] A2. Безпечне очищення ESLint — виконано підзадачами A2.1–A2.11; фінальний baseline 0 errors, 0 warnings
- [COMPLETED] A3. Типізація Supabase User і Chart.js — фактично завершено в межах A2
- [COMPLETED] A4. Аналіз browser-only hydration localStorage і мови — поточну безпечну поведінку збережено та документовано вузькими exceptions

### Фаза B — Користувацькі виправлення та локалізація

- [NEXT] B1. Повна локалізація Annual Report для PL, UK, EN і RU
- [PLANNED] B2. Перевірка правильності чайових на конкретних числових сценаріях і виправлення лише підтверджених проблем
- [PLANNED] B3. Повна локалізація Garage
- [PLANNED] B4. Короткий аудит решти незавершених перекладів UI
- [PLANNED] B5. Порівняння місячного та річного звітів і визначення відсутніх показників

### Фаза C — Продуктовий backlog і точкові технічні покращення

- [PLANNED] C1. Сформувати пріоритетний список нового функціоналу
- [PLANNED] C2. Додавати функції по одній із повною перевіркою
- [DEFERRED] C3. Спільні фінансові типи та розрахункові helpers — лише коли це потрібно конкретній продуктовій задачі
- [DEFERRED] C4. Великий архітектурний рефакторинг — не виконувати без конкретної потреби

### Фаза D — Новий і завершальний функціонал

- [PLANNED] D1. Завершення Garage
- [PLANNED] D2. Повний модуль витрат
- [PLANNED] D3. Податки та Netto
- [PLANNED] D4. Повноцінний Annual Report

### Фаза E — Експорт і мобільна готовність

- [PLANNED] E1. PDF, CSV та можливий Excel-експорт
- [PLANNED] E2. Виділення логіки, придатної для повторного використання
- [PLANNED] E3. Підготовка до мобільного застосунку на Expo React Native

## 7. Поточна активна задача

- Код: B1
- Назва: Повна локалізація Annual Report
- Статус: NEXT
- Реалізація: ще не починалася
- Очікувані мови: PL, UK, EN, RU
- Основний файл: `app/work/year/page.tsx`
- Пов’язані файли перекладів: визначити під час окремого аналізу B1
- Заборонено в межах B1 без окремого погодження: змінювати фінансові формули, Supabase-запити, сортування, графіки, розрахунки або структуру Annual Report

Широких технічних підзадач зараз немає. Наступний крок — окремий аналіз і реалізація B1. Код B1 у межах цієї документаційної задачі не змінюється.

## 8. Журнал прийнятих рішень

- Розробка виконується малими контрольованими етапами.
- Одночасно активна лише одна технічна підзадача.
- Кожен етап має окремий аудит, реалізацію, перевірку і Git-крок.
- Фінансова логіка не змінюється під час lint cleanup.
- Система збереження вибраної мови повинна залишитися сумісною з наявними користувачами.
- Нові колонки Supabase додаються лише через міграції.
- Річний звіт функціонально завершується після витрат і податків.
- Архітектурні зміни виконуються поступово, без великого переписування.
- Нові функції повинні по можливості використовувати спільні типи, сервіси й розрахункові функції.
- Фаза A завершена; актуальний lint baseline становить 0 errors і 0 warnings.
- Широке технічне очищення припиняється; наступний пріоритет — видимі користувачу проблеми та функції.
- A2.10 і A2.11 закриті вузькими документованими ESLint-виключеннями без зміни runtime-поведінки.
- Повний SSR language rendering через cookies не входив до A2.
- Оновлення `<html lang>` буде окремою майбутньою accessibility/SSR-localization задачею.
- Annual Report локалізується до Garage, оскільки його незавершені переклади вже підтверджені користувачем.
- Локалізація не повинна змінювати фінансову логіку.

## 9. Відомі результати попереднього ESLint-аудиту

### Історичний baseline

- Помилки: 23.
- Попередження: 2.

Ці історичні числа більше не описують поточний стан репозиторію.

### Актуальний baseline після A2

- Помилки: 0.
- Попередження: 0.

A2 виконувалася малими окремими підзадачами A2.1–A2.11. Під час lint cleanup фінансові формули не змінювалися.

## 10. Шаблон картки задачі

- Код задачі:
- Назва:
- Мета:
- Початковий стан:
- Що дозволено змінювати:
- Що заборонено змінювати:
- Ризики:
- Файли:
- Автоматичні перевірки:
- Ручний чекліст:
- Критерій завершення:
- Результат:
- Git commit:
- Статус:

## 11. Журнал виконання

### A1 — Roadmap і повторний базовий аудит

- Статус: COMPLETED
- Дата: 2026-07-25
- Зміни: створено `docs/COURIERDASH_ROADMAP.md`
- Код застосунку: не змінювався
- Git commit: не створювався
- Початковий стан Git: гілка `main`, робоче дерево чисте
- Останній commit: `7e3b044 feat(work): add cash tips and refactor platform entry`
- Результат lint: команда не пройшла — 21 помилка і 2 попередження
- Результат build: команда пройшла успішно
- Стан Git після аудиту: єдина зміна — невідстежуваний файл `docs/COURIERDASH_ROADMAP.md`
- Наступна задача: A2 — Безпечне очищення ESLint

### A2 — Безпечне очищення ESLint

- Статус: COMPLETED
- Дата завершення: 2026-07-27
- Початковий актуальний baseline: 21 errors, 2 warnings
- Фінальний baseline: 0 errors, 0 warnings
- Виконано: A2.1–A2.11
- Основні категорії: `prefer-const`, `no-unused-vars`, `no-explicit-any`, Supabase User typing, Chart.js typing, безпечне destructuring, порядок loader/auth helper функцій, документовані browser-only hydration exceptions
- Runtime-поведінка A2.10 і A2.11: не змінювалася
- Фінальні перевірки: lint, typecheck, 3/3 test files, 34/34 tests, production build і ручна production-перевірка
- Перевірені commits:
  - `dbb0e3c fix(garage): use const for percent usage`
  - `fe9df18 fix(work): clean up calculation variables`
  - `61da881 fix(work): remove unused catch binding`
  - `8ad34fd fix(work): use Supabase User type`
  - `81f667b fix(work): type Chart.js datasets`
  - `5a21e09 fix(work): split tax settings result`
  - `576cb4b fix(garage): declare loader before effect`
  - `ce67092 fix(work): declare auth helpers before effect`
  - `190e4e0 fix(year): declare loader before effect`
  - `ced41f3 chore(work): document field settings hydration`
  - `f589cd7 chore(i18n): document language hydration`
- Фінальний commit: `f589cd7c54188bfd79c30efb5b03357c1e64b648`
- Наступна задача: B1 — Повна локалізація Annual Report

## 12. Актуальний базовий аудит

### Стан репозиторію

- Дата аудиту: 2026-07-27
- Поточна гілка: `main`
- HEAD: `f589cd7c54188bfd79c30efb5b03357c1e64b648`
- `main` та `origin/main`: синхронізовані
- Розходження `origin/main...main`: `0 0`
- Робоче дерево до документаційної задачі: чисте

### Актуальний результат ESLint

- `npm run lint`: успішно
- Помилки: 0
- Попередження: 0

Історичні результати попередніх аудитів збережено в секції 9, але вони більше не описують поточний стан.

### Актуальні автоматичні та production-перевірки

- Typecheck: успішно
- Tests: 3/3 test files, 34/34 tests
- Production build: успішно
- Production: A2.11 перевірена користувачем

### Відомий незавершений UI

- В Annual Report перекладається заголовок, але основний вміст залишається українською.
- Annual Report не позначений як повністю локалізований або завершений.

### Наступна задача

Наступна задача: **B1 — Повна локалізація Annual Report**.

Статус B1: `NEXT`. Її аналіз та реалізація в межах цієї документаційної задачі не починалися.
